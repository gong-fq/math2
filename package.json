{
  "name": "math-ai-deepseek",
  "version": "1.1.0",
  "description": "数学核心素养AI - DeepSeek + Desmos + Netlify 部署版",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}